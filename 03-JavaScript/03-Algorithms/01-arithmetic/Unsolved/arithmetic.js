// Fill in the body of each function with the code required

var add = function (num1, num2) {
  return num1 + num2;
};

var subtract = function (num1, num2) {
  return num1 - num2;
};

var multiply = function (num1, num2) {
  return num1 * num2;
};

var divide = function (num1, num2) {
  return num1 / num2;
};

// const data1 = add(2, 4);
// const data2 = subtract(8, 6);
// const data3 = multiply(10, 12);
// const data4 = divide(16, 14);
// console.log(data1, data2, data3, data4);
// subtract(2, 4);
console.log(subtract); // This only prints the definition of the function
console.log(subtract(10, 5)); /* allows user to provide the data for the 
parameters, then prints the solution.   */
